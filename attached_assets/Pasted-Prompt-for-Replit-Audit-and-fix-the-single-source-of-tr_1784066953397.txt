Prompt for Replit:
Audit and fix the single-source-of-truth problem across the prediction engine. Skip any issue that already has an open or completed task solving it (specifically: model-agreement/disagreement classification, paper-trading pipeline, Strong Recommendation thresholds, UI validation-strength language, Data Quality miscalibration above DQ~55, and predicted-set-score winner/loser mismatch — these are already tracked or fixed).
Do not modify prediction probabilities, ensemble weights, calibration, or confidence shrinkage. This is about consistency of display, not model accuracy.
1.	Trace every place modelAgreement, disagreementNote, upsetRisk, eliteTier badge, and recommendation tier are computed or read (ensemble.ts, upsetRisk.ts, eliteTier.ts, recommendation.ts, index.ts, and any UI component). Confirm whether they all read from one finalized prediction object, or whether different components compute/read their own versions independently.
2.	If multiple versions exist, consolidate to one finalized prediction state generated once per match, with every downstream label (Elite badge, disagreement label, upset-risk label, recommendation tier) reading from that same object — never recomputing or re-deriving independently.
3.	Add a final consistency guardrail: before any prediction is displayed or stored, validate that its labels cannot logically contradict each other (e.g., “no model conflict” text alongside “HighDisagreement” label, or “Elite Tier” alongside “Extreme Upset Risk” with no explanation). If a contradiction is detected, block the display and log it rather than showing it to the user.
4.	Run this against the full historical prediction dataset (not a sample) and report: how many past predictions would have failed this guardrail, which components were the source of divergence, and confirm zero contradictions remain after the fix.
5.	Report format: root cause found, files changed, before/after contradiction counts, and any remaining risks. Do not implement silent fixes — report first if the fix requires touching logic outside these files.